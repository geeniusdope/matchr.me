# Supabase

- **`migrations/`** — SQL migrations (create after `supabase init` or when you add schema).
- Project uses **Supabase Cloud** (see [SUPABASE.md](../SUPABASE.md)).

When the CLI is configured, link this folder to your project and push migrations per team process.
